version: '3.8'

services:
  hugo-site:
    build:
      context: .
      dockerfile: Dockerfile.prod
    container_name: hugo-site
    restart: unless-stopped
    ports:
      - "80:80"
    # Optional: Map to custom port
    # ports:
    #   - "8080:80"
